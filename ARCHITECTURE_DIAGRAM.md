# 📐 Realtime Architecture Diagram

Visual representation of how PocketBase realtime integrates with TanStack Query.

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        PocketBase Server                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Database   │  │   Auth       │  │  Realtime    │          │
│  │   (SQLite)   │  │   Service    │  │  Broker      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                            │ │ │
                ┌───────────┘ │ └───────────┐
                │             │             │
           HTTP │        WSS  │        HTTP │
                │             │             │
        ┌───────▼─────┐ ┌────▼─────┐ ┌────▼──────┐
        │  Browser A  │ │ Browser B│ │ Browser C │
        │             │ │          │ │           │
        │ ┌─────────┐ │ │┌────────┐│ │┌─────────┐│
        │ │TanStack │ │ ││TanStack││ ││TanStack ││
        │ │  Query  │ │ ││ Query  ││ ││  Query  ││
        │ │  Cache  │ │ ││ Cache  ││ ││  Cache  ││
        │ └─────────┘ │ │└────────┘│ │└─────────┘│
        └─────────────┘ └──────────┘ └───────────┘
```

## 🔄 Event Flow Diagram

```
User A Creates Patient
        │
        ▼
┌───────────────────┐
│  useMutation      │ ← Your mutation hook
└────────┬──────────┘
         │ HTTP POST
         ▼
┌───────────────────┐
│  PocketBase       │ ← Server receives
│  Creates Record   │
└────────┬──────────┘
         │
         ├─────────────────────────┐
         │                         │
         ▼                         ▼
┌────────────────┐      ┌──────────────────┐
│ Return to      │      │ Broadcast to all │
│ User A (HTTP)  │      │ clients (WSS)    │
└────────┬───────┘      └────────┬─────────┘
         │                       │
         ▼                       ▼
┌────────────────┐      ┌──────────────────┐
│ TanStack Query │      │ User B, C, D...  │
│ onSuccess      │      │ Receive event    │
│ (Browser A)    │      │ (Other browsers) │
└────────────────┘      └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │useRealtimeCollection│ ← Your hook!
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ queryClient      │
                        │ .invalidateQueries│
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ Refetch & Update │
                        │ Cache            │
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ SolidJS Reactive │
                        │ Re-render        │
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ User sees new    │
                        │ patient! ✨      │
                        └──────────────────┘
```

## 🎯 Component Integration

```
┌─────────────────────────────────────────────────────────┐
│              PatientsPage Component                      │
│                                                          │
│  const patients = useCollection('patients')              │
│  useRealtimeCollection('patients')                       │
│                                                          │
│  ┌──────────────────┐  ┌─────────────────────────┐     │
│  │ TanStack Query   │  │ Realtime Subscription   │     │
│  │                  │  │                         │     │
│  │ ┌──────────────┐ │  │ ┌─────────────────────┐ │     │
│  │ │  Query Key   │ │  │ │ PocketBase WebSocket│ │     │
│  │ │ ['patients'] │ │  │ │ subscribe('*')      │ │     │
│  │ └──────────────┘ │  │ └─────────────────────┘ │     │
│  │                  │  │           │             │     │
│  │ ┌──────────────┐ │  │           │ on event   │     │
│  │ │  Cache Data  │◄├──┼───────────┘             │     │
│  │ │  [patients]  │ │  │    invalidate/update    │     │
│  │ └──────────────┘ │  │                         │     │
│  │                  │  │                         │     │
│  └──────────────────┘  └─────────────────────────┘     │
│           │                                              │
│           ▼                                              │
│  ┌──────────────────┐                                   │
│  │  UI Updates      │                                   │
│  │  Automatically   │                                   │
│  └──────────────────┘                                   │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
┌──────────────┐
│    User      │
│  Logs In     │
└──────┬───────┘
       │
       ▼
┌────────────────────┐
│  pb.authStore      │ ← PocketBase auth
│  saves token       │
└────────┬───────────┘
         │
         ├─────────────────────────┐
         │                         │
         ▼                         ▼
┌────────────────┐      ┌──────────────────┐
│  HTTP Requests │      │ WebSocket Connect│
│  Authorization:│      │ Authorization:   │
│  Bearer <token>│      │ Bearer <token>   │
└────────────────┘      └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ PocketBase checks│
                        │ permissions      │
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │ Only send events │
                        │ user can read    │
                        └──────────────────┘
```

## 📊 Cache Update Strategy

```
Realtime Event Received
         │
         ▼
    ┌────────┐
    │ Action?│
    └───┬────┘
        │
    ┌───┴─────────────────────────────┐
    │                                 │
    ▼                                 ▼
┌────────┐                    ┌──────────┐
│CREATE  │                    │ UPDATE   │
└───┬────┘                    └────┬─────┘
    │                              │
    ▼                              ▼
┌────────────────────┐    ┌──────────────────────┐
│ invalidateQueries  │    │ setQueryData         │
│ ['patients','list']│    │ ['patients','detail',│
└────────────────────┘    │  record.id]          │
                          └──────────┬───────────┘
                                     │
                                     ▼
                          ┌──────────────────────┐
                          │ invalidateQueries    │
                          │ ['patients','list']  │
                          └──────────────────────┘

    ┌─────────┐
    │ DELETE  │
    └────┬────┘
         │
         ▼
┌──────────────────────┐
│ removeQueries        │
│ ['patients','detail',│
│  record.id]          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ invalidateQueries    │
│ ['patients','list']  │
└──────────────────────┘
```

## 🎨 Hook Composition

```
Your Component
      │
      ├── useCollection('patients')
      │        │
      │        ├── useQuery()
      │        │     └── returns { data, isLoading, ... }
      │        │
      │        └── queryKey: ['patients', 'list', options]
      │
      └── useRealtimeCollection('patients')
               │
               ├── onMount()
               │     └── pb.collection('patients').subscribe('*')
               │
               ├── Event Handler
               │     ├── event.action === 'create'
               │     │     └── queryClient.invalidateQueries()
               │     │
               │     ├── event.action === 'update'
               │     │     ├── queryClient.setQueryData()
               │     │     └── queryClient.invalidateQueries()
               │     │
               │     └── event.action === 'delete'
               │           ├── queryClient.removeQueries()
               │           └── queryClient.invalidateQueries()
               │
               └── onCleanup()
                     └── unsubscribe()
```

## 🌊 Data Flow Timeline

```
t=0ms    User A clicks "Create Patient"
t=10ms   useMutation.mutate() called
t=20ms   HTTP POST to PocketBase
         │
         ▼
t=100ms  PocketBase creates record
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
t=120ms  Return to A       Broadcast to B,C,D
         │                 │
         ▼                 ▼
t=130ms  A sees result     B,C,D receive WSS event
         (optimistic)      │
                          ▼
t=150ms                    useRealtimeCollection triggered
                          │
                          ▼
t=160ms                    queryClient.invalidateQueries()
                          │
                          ▼
t=180ms                    Cache refetches
                          │
                          ▼
t=200ms                    UI updates
                          │
                          ▼
                    ✨ Users see new patient!
```

## 🎯 Memory Management

```
Component Mounts
      │
      ▼
┌─────────────────┐
│ useRealtimeCollection│
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│ onMount()       │
│ - Create WSS    │
│ - Store unsub   │
└─────────┬───────┘
          │
          ▼
┌─────────────────┐
│ Component Lives │
│ - Receives evts │
│ - Updates cache │
└─────────┬───────┘
          │
          ▼
Component Unmounts
          │
          ▼
┌─────────────────┐
│ onCleanup()     │
│ - Call unsub()  │
│ - Close WSS     │
│ - Free memory   │
└─────────────────┘
```

## 🔄 Multi-User Scenario

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   User A     │  │   User B     │  │   User C     │
│   (Editor)   │  │  (Viewer)    │  │  (Viewer)    │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       │ Updates patient │                 │
       ├─────────────────┼─────────────────┤
       │                 │                 │
       ▼                 ▼                 ▼
┌─────────────────────────────────────────────────┐
│              PocketBase Server                   │
│         (Realtime Broker broadcasts)            │
└────────┬──────────────┬──────────────┬──────────┘
         │              │              │
         ▼              ▼              ▼
┌────────────┐  ┌────────────┐  ┌────────────┐
│ A sees own │  │ B sees      │  │ C sees     │
│ optimistic │  │ realtime    │  │ realtime   │
│ update     │  │ update      │  │ update     │
│ (instant)  │  │ (< 200ms)   │  │ (< 200ms)  │
└────────────┘  └────────────┘  └────────────┘
```

## 📈 Scaling Considerations

```
┌─────────────────────────────────────────────┐
│         PocketBase Realtime Broker           │
│                                              │
│  Max Clients: Unlimited (configurable)       │
│  Memory per Client: ~2-5 MB                  │
│  CPU per Event: Negligible                   │
│                                              │
│  ┌────────────────────────────────────┐     │
│  │   Chunked Broadcasting Strategy    │     │
│  │   (300 clients per batch)          │     │
│  └────────────────────────────────────┘     │
└─────────────────────────────────────────────┘
         │  │  │  │  │  │  │  │  │  │
         ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼
     [Users receive events in parallel]
```

---

## 🎓 Understanding the Flow

1. **User Action** → Triggers mutation
2. **HTTP Request** → PocketBase creates/updates/deletes
3. **WebSocket Broadcast** → All clients notified
4. **Realtime Hook** → Receives event
5. **Cache Update** → TanStack Query syncs
6. **Reactive Update** → SolidJS re-renders
7. **UI Refresh** → Users see changes

**Total time: < 200ms from action to update across all users!** ✨
